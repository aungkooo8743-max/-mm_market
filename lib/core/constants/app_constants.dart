class AppConstants {
  const AppConstants._();

  static const appName = 'MM Market';
  static const defaultCurrency = 'MMK';
  static const defaultCountryCode = '+959';
  static const maxProductImages = 10;
  static const splashDuration = Duration(milliseconds: 800);
}
