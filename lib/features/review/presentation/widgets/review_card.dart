import 'package:flutter/material.dart'; import '../../domain/entities/review.dart';
class ReviewCard extends StatelessWidget{final Review review; final bool canEdit; final VoidCallback? onEdit,onDelete; const ReviewCard({super.key,required this.review,this.canEdit=false,this.onEdit,this.onDelete}); @override Widget build(BuildContext context)=>Card(child:ListTile(title:Text('⭐ ${review.rating.value}/5'),subtitle:Text(review.comment),trailing:canEdit?PopupMenuButton<String>(onSelected:(v){if(v=='edit')onEdit?.call(); if(v=='delete')onDelete?.call();},itemBuilder:(_)=>const [PopupMenuItem(value:'edit',child:Text('Edit')),PopupMenuItem(value:'delete',child:Text('Delete'))]):null));}
